CVPlatform — Merged Production Schema PostgreSQL · TypeORM · UUID PKs everywhere · 29 tables across 6 domain groups Group 1 — Identity users Column Type Constraints id uuid PK email varchar(255) UNIQUE, NOT NULL password_hash varchar(255) NOT NULL first_name varchar(100) NOT NULL last_name varchar(100) NOT NULL role varchar(20) NOT NULL, default &apos;hr&apos; — (admin | hr | manager) department varchar(100) nullable is_active boolean NOT NULL, default true created_at timestamptz auto updated_at timestamptz auto Group 2 — Organisation Structure ℹ️ org_domains replaced by departments (flatter, simpler hierarchy) business_units Column Type Constraints id uuid PK name varchar(255) NOT NULL description text nullable created_at timestamptz auto updated_at timestamptz auto departments Column Type Constraints id uuid PK name varchar(255) NOT NULL business_unit_id uuid FK → business_units.id CASCADE created_at timestamptz auto updated_at timestamptz auto job_roles Column Type Constraints id uuid PK name varchar(255) NOT NULL business_unit_id uuid FK → business_units.id SET NULL, nullable department_id uuid FK → departments.id SET NULL, nullable family_id uuid FK → competence_families.id SET NULL, nullable level smallint default 1 (1–5, overall seniority band) sfia_requirements jsonb default [] — [{ competency_id, required_level }] successor_role_ids uuid[] default {} status varchar(20) default &apos;DRAFT&apos; — (DRAFT | ACTIVE | ARCHIVED) created_by varchar nullable updated_by varchar nullable created_at timestamptz auto updated_at timestamptz auto job_role_levels Column Type Constraints id uuid PK job_role_id uuid FK → job_roles.id CASCADE level_number int NOT NULL (1–7) title varchar(255) NOT NULL mission text nullable responsibilities jsonb default [] — string[] description text nullable created_at timestamptz auto updated_at timestamptz auto Group 3 — Competence Library (SFIA) ℹ️ Legacy competencies + job_competencies tables dropped. Unified on SFIA store. competence_families Column Type Constraints id uuid PK name varchar(200) NOT NULL category enum NOT NULL — (TECHNICAL | BEHAVIORAL | MANAGERIAL) created_at timestamptz auto updated_at timestamptz auto competences Column Type Constraints id uuid PK name varchar(200) NOT NULL description text nullable family_id uuid FK → competence_families.id CASCADE created_at timestamptz auto updated_at timestamptz auto competence_levels Column Type Constraints id uuid PK competence_id uuid FK → competences.id CASCADE level smallint NOT NULL, CHECK 1–5 (1=Aware … 5=Expert) description text NOT NULL job_competency_requirements ℹ️ Added is_required, is_critical, weight vs old schema Column Type Constraints id uuid PK job_role_level_id uuid FK → job_role_levels.id CASCADE competence_id uuid FK → competences.id CASCADE required_level int NOT NULL (1–5) is_required boolean default true is_critical boolean default false weight float default 1.0 created_at timestamptz auto Group 4 — Candidates &amp; CV Pipeline ℹ️ CV pipeline tables retained (cvs, cv_parsed_data, candidate_career_entries). candidate_competencies added as final pipeline output, replacing competency_snapshot jsonb. candidates Column Type Constraints id uuid PK first_name varchar(100) NOT NULL last_name varchar(100) NOT NULL email varchar(255) UNIQUE, NOT NULL phone varchar(20) nullable linkedin_url text nullable current_title varchar(200) nullable years_experience smallint nullable location varchar(150) nullable source varchar(50) nullable gdpr_consent boolean NOT NULL, default false gdpr_consent_at timestamptz nullable status varchar(30) NOT NULL, default &apos;active&apos; notes text nullable created_by varchar nullable created_at timestamptz auto updated_at timestamptz auto cvs Column Type Constraints id uuid PK candidate_id uuid FK → candidates.id CASCADE file_name varchar(255) NOT NULL file_path text NOT NULL mime_type varchar(100) NOT NULL file_hash varchar(64) UNIQUE, nullable — SHA-256 for duplicate detection is_primary boolean default false language varchar(10) default &apos;fr&apos;, nullable parsing_status varchar(20) default &apos;pending&apos; — (pending | done | failed) uploaded_by uuid FK → users.id SET NULL, nullable created_at timestamptz auto cv_parsed_data Column Type Constraints id uuid PK cv_id uuid UNIQUE, NOT NULL — FK → cvs.id raw_text text nullable skills_technical jsonb nullable — string[] skills_soft jsonb nullable — string[] languages jsonb nullable — object[] education jsonb nullable — object[] experience jsonb nullable — object[] total_experience_months smallint nullable llm_summary text nullable parsed_at timestamptz nullable embedding text nullable — vector &apos;[0.1,0.2,...]&apos; for semantic search candidate_career_entries Column Type Constraints id uuid PK candidate_id uuid FK → candidates.id CASCADE role_title varchar(255) NOT NULL company varchar(255) nullable start_date date nullable end_date date nullable sfia_tags jsonb default [] — [{ competencyId, inferredLevel, confidence }] raw_description text nullable source varchar(20) default &apos;AI&apos; — (AI | MANUAL) confidence_score float default 0 created_at timestamptz auto candidate_competencies ℹ️ NEW — populated by CvStorageService as final step after career entries. Replaces candidates.competency_snapshot jsonb. Column Type Constraints id uuid PK candidate_id uuid FK → candidates.id CASCADE competence_id uuid FK → competences.id CASCADE inferred_level smallint NOT NULL (1–5) confidence_score float default 0 source varchar(20) default &apos;AI&apos; — (AI | MANUAL) created_at timestamptz auto updated_at timestamptz auto candidate_notes Column Type Constraints id uuid PK candidate_id uuid FK → candidates.id CASCADE user_id uuid FK → users.id SET NULL, nullable note text NOT NULL rating smallint default 0 (0=none, 1–5 stars) stage varchar(20) default &apos;screening&apos; created_at timestamptz auto Group 5 — Job Offers &amp; ATS Pipeline job_offers Column Type Constraints id uuid PK job_role_id uuid FK → job_roles.id SET NULL, nullable job_role_level_id uuid FK → job_role_levels.id SET NULL, nullable title varchar(200) NOT NULL description text NOT NULL hiring_manager_id uuid FK → employees.id SET NULL, nullable contract_type varchar(50) nullable work_mode varchar(50) nullable salary_min decimal(12,2) nullable salary_max decimal(12,2) nullable currency varchar(10) default &apos;TND&apos; openings_count integer default 1 deadline timestamp nullable snapshot jsonb default {} — role requirements snapshot at creation scoring_weights jsonb NOT NULL — { cv, assessfirst, interview, competency } email_settings jsonb NOT NULL — email automation config status varchar(50) default &apos;open&apos; visibility varchar(50) default &apos;both&apos; created_by varchar nullable created_at timestamptz auto updated_at timestamptz auto applications ℹ️ Added match_score column. applications remains the central hub — all pipeline entities hang off it. Column Type Constraints id uuid PK job_id uuid FK → job_offers.id CASCADE candidate_id uuid FK → candidates.id CASCADE stage varchar(50) default &apos;applied&apos; — (applied | screening | interview | assessment | offer | rejected) source varchar(100) nullable cover_note text nullable match_score float nullable — computed overall fit score competency_gap jsonb nullable — gap analysis cache created_at timestamptz auto updated_at timestamptz auto application_competency_scores ℹ️ Now uses competence_id (SFIA) instead of old competency_id. Added expected_level and gap columns. Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE competence_id uuid FK → competences.id CASCADE evaluated_level smallint NOT NULL (1–5) expected_level smallint nullable gap smallint nullable — expected_level - evaluated_level rated_by uuid FK → users.id SET NULL created_at timestamptz auto updated_at timestamptz auto application_assessfirst ℹ️ Retained as application-scoped AssessFirst store. Legacy assessfirst_results (candidate-scoped) dropped. Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE, UNIQUE uploaded_by uuid FK → users.id SET NULL, nullable candidate_name varchar(100) nullable assessment_date varchar(20) nullable personal_style varchar(100) nullable personal_style_desc text nullable traits jsonb default [] improvements jsonb default [] talent_cloud jsonb default {} dimension_details jsonb default {} top_motivators jsonb default [] low_motivators jsonb default [] preferred_activities jsonb default [] management_style jsonb default [] sought_management jsonb default [] culture_fit varchar(50) nullable culture_desc text nullable decision_making varchar(100) nullable preferred_tasks varchar(100) nullable learning_style varchar(100) nullable aptitude_desc text nullable created_at timestamptz auto updated_at timestamptz auto application_notes Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE user_id uuid FK → users.id SET NULL note text NOT NULL rating smallint default 0 (0=none, 1–5 stars) stage varchar(50) nullable created_at timestamptz auto activity_log Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE user_id uuid FK → users.id SET NULL action varchar(100) NOT NULL — (application_created | stage_changed | note_added | assessfirst_uploaded | score_calculated | email_sent | interview_scheduled | interview_completed | task_created | task_completed | competency_rated) description text nullable metadata jsonb nullable — e.g. { from: &apos;applied&apos;, to: &apos;screening&apos; } created_at timestamptz auto interviews Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE scheduled_at timestamptz NOT NULL type varchar(50) NOT NULL — (HR | Technical | Final) interviewer_name varchar(200) NOT NULL status varchar(50) default &apos;scheduled&apos; — (scheduled | completed | cancelled) meeting_url varchar(512) nullable technical_score smallint nullable (1–5) communication_score smallint nullable (1–5) comments text nullable decision varchar(50) nullable — (pass | fail | maybe) created_at timestamptz auto updated_at timestamptz auto tasks Column Type Constraints id uuid PK application_id uuid FK → applications.id CASCADE title varchar(255) NOT NULL due_date timestamptz nullable priority varchar(50) default &apos;medium&apos; — (low | medium | high) completed boolean default false created_at timestamptz auto updated_at timestamptz auto Group 6 — Employees &amp; Internal Mobility ℹ️ employees gains department_id FK (replacing org-domain link). employee_competencies enriched with target_level, gap, gap_percentage. Two new talent marketplace tables added. employees Column Type Constraints id uuid PK employee_id varchar UNIQUE, NOT NULL candidate_id uuid FK → candidates.id SET NULL, nullable (OneToOne) user_id uuid FK → users.id SET NULL, nullable (OneToOne) first_name varchar(100) NOT NULL last_name varchar(100) NOT NULL email varchar(255) UNIQUE, NOT NULL hire_date date NOT NULL status enum NOT NULL, default &apos;probation&apos; — (probation | active | notice | retired | terminated) job_role_id uuid FK → job_roles.id job_role_level_id uuid FK → job_role_levels.id department_id uuid FK → departments.id SET NULL, nullable manager_id uuid self-referential FK (materialized-path tree) personal_details jsonb nullable llm_summary text nullable embedding text nullable — vector for semantic search created_at timestamptz auto updated_at timestamptz auto employee_competencies Column Type Constraints id uuid PK employee_id uuid FK → employees.id CASCADE competence_id uuid FK → competences.id CASCADE current_level int default 1 (1–5) target_level int nullable (1–5) gap int nullable — target_level - current_level gap_percentage float nullable is_required boolean default false last_evaluated_at timestamptz nullable source enum default &apos;manual&apos; — (assessment_import | manual | performance_review) created_at timestamptz auto updated_at timestamptz auto employee_role_matches ℹ️ NEW — pre-computed internal mobility scores for talent marketplace. Column Type Constraints id uuid PK employee_id uuid FK → employees.id CASCADE job_role_id uuid FK → job_roles.id CASCADE job_role_level_id uuid FK → job_role_levels.id CASCADE match_score float NOT NULL (0–100) readiness varchar(20) NOT NULL — (ready | near_ready | gap) competency_gap jsonb nullable — detailed gap breakdown calculated_at timestamptz auto internal_applications ℹ️ NEW — employees applying to internal job offers. Column Type Constraints id uuid PK employee_id uuid FK → employees.id CASCADE job_offer_id uuid FK → job_offers.id CASCADE status varchar(50) default &apos;applied&apos; — (applied | reviewing | accepted | rejected) created_at timestamptz auto updated_at timestamptz auto Summary of Changes vs Previous Schema Change Detail REMOVED org_domains → replaced by departments REMOVED competencies + job_competencies (legacy store) → unified on SFIA REMOVED assessfirst_results (candidate-scoped legacy) → application_assessfirst retained REMOVED candidates.competency_snapshot jsonb → replaced by candidate_competencies table ADDED departments — flat, direct BU link ADDED candidate_competencies — final CV pipeline output (replaces snapshot jsonb) ADDED employee_role_matches — talent marketplace internal mobility scores ADDED internal_applications — employee-to-offer internal pipeline ENRICHED job_competency_requirements + is_required, is_critical, weight ENRICHED employee_competencies + target_level, gap, gap_percentage, is_required, last_evaluated_at ENRICHED application_competency_scores + expected_level, gap (now on SFIA competence_id) ENRICHED applications + match_score ENRICHED employees + department_id FK RETAINED Full CV pipeline: cvs, cv_parsed_data, candidate_career_entries
